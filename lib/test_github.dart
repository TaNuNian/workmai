import 'package:flutter/material.dart';

class TestGithub extends StatelessWidget {
  const TestGithub({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'PLEASE GITHUB PLEASEEEEEEEEEE'
        ),
      ),
    );
  }
}
